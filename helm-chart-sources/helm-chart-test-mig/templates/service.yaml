apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-test-mig.fullname" . }}
  labels:
    {{- include "helm-chart-test-mig.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-chart-test-mig.selectorLabels" . | nindent 4 }}
