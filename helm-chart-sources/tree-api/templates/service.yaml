apiVersion: v1
kind: Service
metadata:
  name: {{ include "tree-api.fullname" . }}
  labels:
    {{- include "tree-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tree-api.selectorLabels" . | nindent 4 }}
